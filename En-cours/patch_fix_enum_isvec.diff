diff -u a/ggml/src/ggml-metal/ggml-metal-device.m b/ggml/src/ggml-metal/ggml-metal-device.m
--- a/ggml/src/ggml-metal/ggml-metal-device.m
+++ b/ggml/src/ggml-metal/ggml-metal-device.m
@@ -1193,7 +1193,7 @@
                 if (op->src[1]->type != op->src[2]->type) {
                     return false;
                 }

-                const ggml_type tkv = op->src[1]->type;
+                const enum ggml_type tkv = op->src[1]->type;
                 if (!(tkv == GGML_TYPE_F16 || tkv == GGML_TYPE_BF16 || ggml_is_quantized(tkv))) {
                     return false;
                 }
diff -u a/ggml/src/ggml-metal/ggml-metal-ops.cpp b/ggml/src/ggml-metal/ggml-metal-ops.cpp
--- a/ggml/src/ggml-metal/ggml-metal-ops.cpp
+++ b/ggml/src/ggml-metal/ggml-metal-ops.cpp
@@ -2288,6 +2288,10 @@
     // NOTE: keep a local flag for vec vs blk path selection
+    const bool is_vec = ggml_metal_op_flash_attn_ext_use_vec(op);
+    (void) is_vec;

     // ... existing code continues ...
@@ -2296,7 +2300,7 @@
-        (void) is_vec;
+        /* is_vec already referenced above */
